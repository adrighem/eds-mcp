# Standing Rules

- Do not take public GitHub actions without explicit human approval.
- Do not merge external PRs directly.
- For dependency maintenance, use `uv` to regenerate lockfile entries and run tests before recommending closure or follow-up action.
- For code changes touching MCP outputs, keep responses compact and structured.

